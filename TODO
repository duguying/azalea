refactor hashtable
--let the hashtable support muilt type
--and then refactor the pool

pool

config: equation

#----------------------
# Communication
socket package
websocket model

# Parser
message transport protocol
message parser

# Database Driver
mysql driver
sqlite driver

# Log

# Tcp Client Tester
shutdown when server closed the socket


# attention
we should design connection pool before the message exchange in the core

#----------------------
HAVEDONE

2013-12-17
refactor hashtable finished, combine the two node,
node in hashtable(not hashnode) and node in stack
2013-12-30
sock_connect()
暂时移除load_model
暂时修复pool，后续需要完全重构
