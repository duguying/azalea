then refactor the pool

pool

config: equation

# Tcp Client Tester
shutdown when server closed the socket

#----------------------

# About client library
将client单独所需的函数打包到libclient


# Communication
socket package
websocket model

# Parser
message transport protocol
message parser

# Database Driver
mysql driver
sqlite driver

# attention
we should design connection pool before the message exchange in the core

#----------------------
HAVEDONE

2013-12-17
refactor hashtable finished, combine the two node,
node in hashtable(not hashnode) and node in stack
2013-12-30
sock_connect()
暂时移除load_model
暂时修复pool，后续需要完全重构
2014-01-27
ds支持复合类型及struct类型节点
将数据传输打包到基本数据帧，数据由最小数据传输单位(帧)来传输
